diff --git a/code/handlers/stock_handler.py b/code/handlers/stock_handler.py
index 691d0bd..807450c 100644
--- a/code/handlers/stock_handler.py
+++ b/code/handlers/stock_handler.py
@@ -22,9 +22,8 @@ from qlib.contrib.data.handler import Alpha158
 # ==========================================================================
 EXTRA_EXPR_FEATURES: list[tuple[str, str]] = [
     # --- 基础字段（供自定义 Processor 使用） ---
+    # 注意：HIGH0/LOW0 已在 Alpha158 中定义，此处不重复添加
     ("$close", "CLOSE0"),
-    ("$high", "HIGH0"),
-    ("$low", "LOW0"),
     ("$volume", "VOLUME0"),
     ("$amount", "AMOUNT0"),
     # --- 行业分类（已写入 Qlib 二进制数据的静态特征） ---
@@ -112,4 +111,4 @@ class StockDataHandler(Alpha158):
 
     def get_label_config(self) -> tuple[list[str], list[str]]:
         """覆盖 Alpha158 默认标签，使用比赛要求的 5 日开盘收益率。"""
-        return LABEL_EXPR, LABEL_NAME
\ No newline at end of file
+        return LABEL_EXPR, LABEL_NAME
diff --git a/code/models/LightGBM/LightGBM.yaml b/code/models/LightGBM/LightGBM.yaml
index f66921a..8cf9a0a 100644
--- a/code/models/LightGBM/LightGBM.yaml
+++ b/code/models/LightGBM/LightGBM.yaml
@@ -5,7 +5,7 @@ qlib_init:
     class: MLflowExpManager
     module_path: qlib.workflow.expm
     kwargs:
-      uri: file://./temp/mlruns
+      uri: "file:./temp/mlruns"
       default_exp_name: lgbm_test
 
 task:
@@ -66,11 +66,20 @@ task:
               kwargs:
                 fields_group: "feature"
           learn_processors:
+            - class: FridayFilterProcessor
+              module_path: "code.processors.custom_processor"
             - class: DropnaLabel
+            - class: RobustZScoreNorm
+              kwargs:
+                fields_group: "feature"
+                clip_outlier: True
+            - class: Fillna
+              kwargs:
+                fields_group: "feature"
       segments:
         train: ["2024-01-01", "2026-02-10"]
-        valid: ["2026-02-11", "2026-04-10"]
-        test:  ["2026-04-13", "2026-04-17"]
+        valid: ["2026-02-11", "2026-04-03"]
+        test:  ["2026-04-07", "2026-04-10"]
 
   record:
     - class: SignalRecord
diff --git a/code/models/PointwiseStockTransformer/PointwiseStockTransformer.py b/code/models/PointwiseStockTransformer/PointwiseStockTransformer.py
index f0ffaf1..6ffd023 100644
--- a/code/models/PointwiseStockTransformer/PointwiseStockTransformer.py
+++ b/code/models/PointwiseStockTransformer/PointwiseStockTransformer.py
@@ -330,6 +330,9 @@ class PointwiseTransformerModel(Model):
             feat = batch[..., :-1]
             target = batch[:, -1, -1]  # 最后一天对应的 label
 
+            # 替换特征中的 NaN 为 0，避免 Transformer 前向传播产生 NaN
+            feat = torch.nan_to_num(feat, nan=0.0)
+
             if training:
                 optimizer.zero_grad()
             with torch.no_grad() if not training else torch.enable_grad():
@@ -374,10 +377,12 @@ class PointwiseTransformerModel(Model):
                     batch = torch.from_numpy(batch)
                 batch = batch.float().to(self.device)
                 feat = batch[..., :-1]
+                # 替换特征中的 NaN 为 0，避免 Transformer 前向传播产生 NaN
+                feat = torch.nan_to_num(feat, nan=0.0)
                 out = self._net(feat).detach().cpu().numpy()
                 preds.append(out)
 
         pred_arr = np.concatenate(preds, axis=0)
         # TSDataSampler 保留原始 (datetime, instrument) MultiIndex
         index = test_ds.get_index()
-        return pd.Series(pred_arr, index=index, name="score")
+        return pd.Series(pred_arr, index=index, name="score")
\ No newline at end of file
diff --git a/code/models/PointwiseStockTransformer/PointwiseStockTransformer.yaml b/code/models/PointwiseStockTransformer/PointwiseStockTransformer.yaml
index 6c3ea77..4a9c652 100644
--- a/code/models/PointwiseStockTransformer/PointwiseStockTransformer.yaml
+++ b/code/models/PointwiseStockTransformer/PointwiseStockTransformer.yaml
@@ -1,11 +1,11 @@
-﻿qlib_init:
+qlib_init:
   provider_uri: "./temp/qlib_data"
   region: cn
   exp_manager:
     class: MLflowExpManager
     module_path: qlib.workflow.expm
     kwargs:
-      uri: file://./temp/mlruns
+      uri: "file:./temp/mlruns"
       default_exp_name: pointwise_transformer_test
 
 task:
@@ -74,10 +74,17 @@ task:
                 fields_group: "feature"
           learn_processors:
             - class: DropnaLabel
+            - class: RobustZScoreNorm
+              kwargs:
+                fields_group: "feature"
+                clip_outlier: True
+            - class: Fillna
+              kwargs:
+                fields_group: "feature"
       segments:
         train: ["2024-01-01", "2026-02-10"]
-        valid: ["2026-02-11", "2026-04-10"]
-        test:  ["2026-04-13", "2026-04-17"]
+        valid: ["2026-02-11", "2026-04-03"]
+        test:  ["2026-04-07", "2026-04-10"]
       step_len: 60
       fillna_type: "ffill+bfill"
 
diff --git a/code/processors/custom_processor.py b/code/processors/custom_processor.py
index 755439b..a98e98e 100644
--- a/code/processors/custom_processor.py
+++ b/code/processors/custom_processor.py
@@ -27,9 +27,65 @@ def get_group_columns(df: pd.DataFrame, group: str) -> pd.Index:
     例如 ('feature', 'CLOSE0')。
     """
     if isinstance(df.columns, pd.MultiIndex):
-        return df.columns[df.columns.get_level_values(0) == group]
+        level0 = df.columns.get_level_values(0)
+        # 处理 NaN 值：fillna 后再比较，避免 NaN 导致布尔掩码错误
+        mask = level0.fillna("") == group if hasattr(level0, 'fillna') else level0 == group
+        return df.columns[mask]
     # 对于单级列索引，尝试匹配以 fields_group 同名的前缀
-    return df.columns[df.columns.str.startswith(f"{group}_") if hasattr(df.columns, 'str') else df.columns == group]
+    str_cols = df.columns.astype(str)
+    return df.columns[str_cols.str.startswith(f"{group}_")]
+
+
+def _get_feat_col(df: pd.DataFrame, col_name: str, fields_group: str = "feature") -> pd.Series:
+    """从 qlib MultiIndex DataFrame 中安全获取指定列名的 Series。
+    
+    支持两种格式：
+    1. MultiIndex: ('feature', 'CLOSE0') -> 使用 xs
+    2. 单级索引: 'CLOSE0' -> 直接访问
+    """
+    if isinstance(df.columns, pd.MultiIndex):
+        # 使用 get_loc 在第二级查找，返回精确的列位置
+        level1 = df.columns.get_level_values(1)
+        mask = level1 == col_name
+        if mask.any():
+            result = df.iloc[:, mask]
+            if result.shape[1] == 1:
+                return result.iloc[:, 0]
+            # 如果有多个匹配（不同 group），取指定 group 的
+            level0 = df.columns.get_level_values(0)
+            group_mask = mask & (level0 == fields_group)
+            if group_mask.any():
+                return df.iloc[:, group_mask].iloc[:, 0]
+            return result.iloc[:, 0]
+        raise KeyError(f"Column '{col_name}' not found in DataFrame columns: {df.columns.tolist()}")
+    return df[col_name]
+
+
+def _get_feat_col_single(df: pd.DataFrame, col_name: str) -> pd.Series:
+    """从单 instrument 子 DataFrame 中获取列值，返回 1D Series。
+    
+    处理 MultiIndex 列的情况：当 groupby 后，子 DataFrame 仍保留 MultiIndex 列。
+    """
+    if isinstance(df.columns, pd.MultiIndex):
+        # 尝试在第二级查找
+        level1_vals = df.columns.get_level_values(1)
+        mask = level1_vals == col_name
+        if mask.any():
+            return df.iloc[:, mask].iloc[:, 0]
+        # 尝试在第一级查找
+        level0_vals = df.columns.get_level_values(0)
+        mask0 = level0_vals == col_name
+        if mask0.any():
+            return df.iloc[:, mask0].iloc[:, 0]
+        raise KeyError(f"Column '{col_name}' not found in MultiIndex columns: {list(df.columns)}")
+    return df[col_name]
+
+
+def _to_multiindex(df: pd.DataFrame, fields_group: str) -> pd.DataFrame:
+    """将单级列 DataFrame 转换为 MultiIndex 列结构。"""
+    df = df.copy()
+    df.columns = pd.MultiIndex.from_product([[fields_group], df.columns])
+    return df
 
 
 # ---------------------------------------------------------------------------
@@ -61,9 +117,9 @@ class MomentumProcessor(Processor):
 
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].values.astype(float)
-            high = sub["HIGH0"].values.astype(float)
-            low = sub["LOW0"].values.astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").values.astype(float)
+            high = _get_feat_col_single(sub, "HIGH0").values.astype(float)
+            low = _get_feat_col_single(sub, "LOW0").values.astype(float)
 
             _, macd_signal, macd_hist = talib.MACD(close)
             rsi = talib.RSI(close, timeperiod=14)
@@ -79,6 +135,7 @@ class MomentumProcessor(Processor):
 
         extra = pd.DataFrame({n: pd.concat(all_series[n]).sort_index() for n in names})
         extra = extra.reindex(feat.index).replace([np.inf, -np.inf], np.nan)
+        extra = _to_multiindex(extra, self.fields_group)
         return pd.concat([df, extra], axis=1)
 
     def readonly(self) -> bool:
@@ -108,15 +165,18 @@ class VolatilityProcessor(Processor):
         series_list = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            atr = talib.ATR(sub["HIGH0"].values.astype(float),
-                            sub["LOW0"].values.astype(float),
-                            sub["CLOSE0"].values.astype(float),
-                            timeperiod=14)
+            atr = talib.ATR(
+                _get_feat_col_single(sub, "HIGH0").values.astype(float),
+                _get_feat_col_single(sub, "LOW0").values.astype(float),
+                _get_feat_col_single(sub, "CLOSE0").values.astype(float),
+                timeperiod=14,
+            )
             series_list.append(pd.Series(atr, index=sub.index, name="ATR14"))
 
         atr_series = pd.concat(series_list).sort_index().reindex(feat.index)
         atr_series.replace([np.inf, -np.inf], np.nan, inplace=True)
-        return pd.concat([df, atr_series], axis=1)
+        extra = _to_multiindex(atr_series.to_frame(), self.fields_group)
+        return pd.concat([df, extra], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -145,13 +205,16 @@ class VolumeProcessor(Processor):
         obv_list = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            obv = talib.OBV(sub["CLOSE0"].values.astype(float),
-                            sub["VOLUME0"].values.astype(float))
+            obv = talib.OBV(
+                _get_feat_col_single(sub, "CLOSE0").values.astype(float),
+                _get_feat_col_single(sub, "VOLUME0").values.astype(float),
+            )
             obv_list.append(pd.Series(obv, index=sub.index, name="OBV"))
 
         obv_all = pd.concat(obv_list).sort_index().reindex(feat.index)
         obv_all.replace([np.inf, -np.inf], np.nan, inplace=True)
-        return pd.concat([df, obv_all], axis=1)
+        extra = _to_multiindex(obv_all.to_frame(), self.fields_group)
+        return pd.concat([df, extra], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -182,14 +245,15 @@ class MomentumQualityProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
             ret1 = close.pct_change(1)
             ret20 = close.pct_change(20)
             vol20 = ret1.rolling(20).std()
             quality = (ret20 / vol20.replace(0, np.nan)).replace([np.inf, -np.inf], np.nan)
             results.append(pd.DataFrame({"ADV_MOM_QUALITY_20": quality}, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -213,7 +277,7 @@ class TailRiskProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
             ret1 = close.pct_change(1)
             downside = ret1.clip(upper=0).rolling(20).std()
             maxdd = (close / close.rolling(20).max() - 1.0)
@@ -222,7 +286,8 @@ class TailRiskProcessor(Processor):
                 "ADV_MAX_DD_20": maxdd,
             }, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -246,7 +311,7 @@ class DistributionProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
             ret1 = close.pct_change(1)
             skew = ret1.rolling(20).skew()
             kurt = ret1.rolling(20).kurt()
@@ -255,7 +320,8 @@ class DistributionProcessor(Processor):
                 "ADV_KURT_20": kurt,
             }, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -280,9 +346,12 @@ class LiquidityProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
-            volume = sub["VOLUME0"].astype(float)
-            amount = sub.get("AMOUNT0", pd.Series(np.nan, index=sub.index)).astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
+            volume = _get_feat_col_single(sub, "VOLUME0").astype(float)
+            try:
+                amount = _get_feat_col_single(sub, "AMOUNT0").astype(float)
+            except KeyError:
+                amount = pd.Series(np.nan, index=sub.index)
 
             ret1 = close.pct_change(1)
             amihud = (ret1.abs() / amount.replace(0, np.nan)).rolling(20).mean()
@@ -293,7 +362,8 @@ class LiquidityProcessor(Processor):
                 "ADV_TURNOVER_RATIO_20_60": turnover,
             }, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -317,7 +387,7 @@ class PricePositionProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
             roll_max = close.rolling(60).max()
             roll_min = close.rolling(60).min()
             pos = (close - roll_min) / (roll_max - roll_min + 1e-12)
@@ -330,7 +400,8 @@ class PricePositionProcessor(Processor):
                 "ADV_DIST_TO_LOW_60": dist_low,
             }, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -355,9 +426,9 @@ class VolatilityStructureProcessor(Processor):
         results = []
         for instrument, sub in feat.groupby(level="instrument", sort=False):
             sub = sub.sort_index()
-            close = sub["CLOSE0"].astype(float)
-            high = sub["HIGH0"].astype(float)
-            low = sub["LOW0"].astype(float)
+            close = _get_feat_col_single(sub, "CLOSE0").astype(float)
+            high = _get_feat_col_single(sub, "HIGH0").astype(float)
+            low = _get_feat_col_single(sub, "LOW0").astype(float)
 
             ret1 = close.pct_change(1)
             vol5 = ret1.rolling(5).std()
@@ -372,7 +443,8 @@ class VolatilityStructureProcessor(Processor):
                 "ADV_HL_RANGE_MEAN_20": hl_mean,
             }, index=sub.index))
         out = pd.concat(results).sort_index().reindex(feat.index)
-        return pd.concat([df, _replace_inf(out)], axis=1)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
+        return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
         return False
@@ -396,7 +468,7 @@ class CrossSectionalRankProcessor(Processor):
         if "RET5" not in cols.get_level_values(-1):
             return df
 
-        ret5 = feat["RET5"]
+        ret5 = _get_feat_col(feat, "RET5", self.fields_group)
         rank = ret5.groupby(level="datetime").rank(pct=True)
         mean = ret5.groupby(level="datetime").transform("mean")
         std = ret5.groupby(level="datetime").transform("std")
@@ -406,7 +478,7 @@ class CrossSectionalRankProcessor(Processor):
             "CS_RANK_RET5": rank,
             "CS_ZSCORE_RET5": zscore,
         }, index=feat.index)
-        out.replace([np.inf, -np.inf], np.nan, inplace=True)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
         return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
@@ -429,9 +501,9 @@ class MarketLevelProcessor(Processor):
         if not {"RET1", "RET5", "RET10"}.issubset(col_set):
             return df
 
-        ret1 = feat["RET1"]
-        ret5 = feat["RET5"]
-        ret10 = feat["RET10"]
+        ret1 = _get_feat_col(feat, "RET1", self.fields_group)
+        ret5 = _get_feat_col(feat, "RET5", self.fields_group)
+        ret10 = _get_feat_col(feat, "RET10", self.fields_group)
 
         out = pd.DataFrame({
             "MARKET_MOM_5": ret5.groupby(level="datetime").transform("mean"),
@@ -441,7 +513,7 @@ class MarketLevelProcessor(Processor):
             ),
             "MARKET_DISPERSION": ret1.groupby(level="datetime").transform("std"),
         }, index=feat.index)
-        out.replace([np.inf, -np.inf], np.nan, inplace=True)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
         return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
@@ -472,10 +544,10 @@ class SectorLevelProcessor(Processor):
                 df[c] = 0.0
             return df
 
-        ret1 = feat["RET1"]
-        ret5 = feat["RET5"]
-        ret10 = feat["RET10"]
-        sector = feat["sector_l1"].astype(int).astype(str)
+        ret1 = _get_feat_col(feat, "RET1", self.fields_group)
+        ret5 = _get_feat_col(feat, "RET5", self.fields_group)
+        ret10 = _get_feat_col(feat, "RET10", self.fields_group)
+        sector = _get_feat_col(feat, "sector_l1", self.fields_group).astype(int).astype(str)
 
         out = pd.DataFrame(index=feat.index)
         for suffix, ret in [("5", ret5), ("10", ret10)]:
@@ -492,8 +564,24 @@ class SectorLevelProcessor(Processor):
             [feat.index.get_level_values("datetime"), sector]
         ).transform(lambda x: (x > 0).mean())
 
-        out.replace([np.inf, -np.inf], np.nan, inplace=True)
+        out = _to_multiindex(_replace_inf(out), self.fields_group)
         return pd.concat([df, out], axis=1)
 
     def readonly(self) -> bool:
-        return False
\ No newline at end of file
+        return False
+
+
+class FridayFilterProcessor(Processor):
+    """仅保留每周五的样本，确保周频标签对齐。"""
+    def fit(self, df: pd.DataFrame = None) -> "FridayFilterProcessor":
+        return self
+
+    def __call__(self, df: pd.DataFrame) -> pd.DataFrame:
+        # df 的 index 是 (datetime, instrument) 的 MultiIndex
+        if isinstance(df.index, pd.MultiIndex):
+            dates = df.index.get_level_values("datetime")
+        else:
+            dates = df.index
+        # 只保留星期五 (weekday == 4)
+        mask = dates.dayofweek == 4
+        return df[mask]
\ No newline at end of file
diff --git a/code/src/run_all_model.py b/code/src/run_all_model.py
index 8353e81..7836e0c 100644
--- a/code/src/run_all_model.py
+++ b/code/src/run_all_model.py
@@ -269,7 +269,8 @@ def build_weekly_rolling_tasks(
         segs = task["dataset"]["kwargs"]["segments"]
         segs["train"] = (ROLLING_TRAIN_START, train_end)
         segs["valid"] = (w_start, w_end)
-        segs.pop("test", None)  # 移除 test（如果存在）
+        # 保留 test 段指向 valid 的日期，因为 SignalRecord.generate() 会调用 model.predict(dataset, segment="test")
+        segs["test"] = (w_start, w_end)
 
         # 同步 handler 时间
         handler_cfg = task["dataset"]["kwargs"]["handler"]
@@ -331,12 +332,10 @@ def generate_records(
     # PortAnaRecord 需要从 record kwargs 或 task/full 配置中获取组合分析配置，
     # 这里统一解析该配置。
     port_config = port_kwargs.pop("config", None) or get_port_analysis_config(task, full_cfg, port_record_cfg)
-    if port_config is None:
-        raise KeyError(
-            "PortAnaRecord requires a `config`. Put it in `task.port_analysis_config`, "
-            "`port_analysis_config`, or `task.record[].kwargs.config`."
-        )
-    PortAnaRecord(recorder=recorder, config=port_config, **port_kwargs).generate()
+    if port_config is not None:
+        PortAnaRecord(recorder=recorder, config=port_config, **port_kwargs).generate()
+    else:
+        print("Skipping PortAnaRecord: no port_analysis_config provided.")
 
     for record_cfg in task.get("record", []) or []:
         class_name = record_class_name(record_cfg)
@@ -560,4 +559,4 @@ class ModelRunner:
 
 
 if __name__ == "__main__":
-    fire.Fire(ModelRunner)
+    fire.Fire(ModelRunner)
\ No newline at end of file
